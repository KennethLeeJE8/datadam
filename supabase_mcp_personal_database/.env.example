# MCP Server Configuration
MCP_TRANSPORT=stdio  # or 'http' for HTTP transport
PORT=3000           # HTTP server port (Render assigns this automatically)

# Database Configuration (Required)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_ANON_KEY=your-anon-key

# Security Configuration (Optional for HTTP transport)
# API key authentication (if not set, authentication is disabled)
MCP_API_KEY=your-secret-api-key-here

# CORS Configuration
# Comma-separated list of allowed origins, supports wildcards
CORS_ORIGINS=http://localhost:*,https://localhost:*,chrome-extension://*

# Rate Limiting (Render tier restrictions)
RATE_LIMIT_REQUESTS=50      # Max requests per window (reduced for limited resources)
RATE_LIMIT_WINDOW=900000    # Window in milliseconds (15 minutes)

# Request Configuration (Render tier restrictions)
REQUEST_TIMEOUT=30000       # Request timeout in milliseconds (30 seconds)
MAX_REQUEST_SIZE=100kb      # Maximum request body size (Render limit)

# Connection Management (Render tier restrictions)
MAX_CONNECTIONS=3           # Maximum concurrent connections
CACHE_SIZE=100              # Maximum cache entries

# Application Environment
NODE_ENV=development        # development, test, or production

# ====================================
# ADDITIONAL PRODUCTION SETTINGS
# ====================================

# Health check endpoint path (default: /health)
# HEALTH_ENDPOINT=/health

# Metrics endpoint path (default: /metrics)
# METRICS_ENDPOINT=/metrics

# Log level: debug, info, warn, error
# LOG_LEVEL=info

# Enable request logging
# ENABLE_REQUEST_LOGGING=true